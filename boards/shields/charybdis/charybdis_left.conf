# === ESB peripheral 설정 (left) ===
# peripheral은 0부터 시작하는 고유 정수 ID. left=0, right=1.
CONFIG_ZMK_SPLIT_ESB_PERIPHERAL_ID=0

# TX ring buffer 크기 (저자 권장값 — 입력 버스트 버퍼링/ACK 안정화)
CONFIG_ESB_TX_RINGBUF_SIZE=400
