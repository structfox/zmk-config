# === USB ===
CONFIG_ZMK_USB=y

# === 기본 설정 ===
CONFIG_ZMK_KSCAN_DEBOUNCE_PRESS_MS=5
CONFIG_ZMK_KSCAN_DEBOUNCE_RELEASE_MS=5

# === 포인팅 ===
# macOS에서 HID Resolution Multiplier로 마우스 입력이 멈추는 문제로 비활성화
CONFIG_ZMK_POINTING_SMOOTH_SCROLLING=n

# === 전력 관리 ===
CONFIG_ZMK_SLEEP=y
CONFIG_ZMK_IDLE_TIMEOUT=60000
CONFIG_ZMK_IDLE_SLEEP_TIMEOUT=1200000

# === BLE 최적화 ===
# +3 dBm: 0 dBm 마진 부족 / +8 dBm 발열·SNR 악화의 절충안
CONFIG_BT_CTLR_TX_PWR_PLUS_3=y

# === BLE 처리량 강화 ===
# 2 Mbps PHY — EXPERIMENTAL_FEATURES 가 EXPERIMENTAL_CONN 을 select 하면서
# ZMK 가 PHY_2M default 를 n 으로 덮어쓰므로 반드시 명시 (없으면 1 Mbps 로 떨어져 split 큐 적체)
CONFIG_BT_CTLR_PHY_2M=y
# GATT MTU 자동 협상 (큰 attribute 효율 송수신)
CONFIG_BT_GATT_AUTO_UPDATE_MTU=y

# === 딥슬립 후 BLE 재연결 개선 ===
# 커뮤니티 컨센서스: split BLE PREF / BT_PERIPHERAL_PREF 는 ZMK 기본값(6/30/400) 유지
# FEATURES 가 CONN/SEC 둘 다 select 하므로 EXPERIMENTAL_CONN 별도 명시 불필요
CONFIG_ZMK_BLE_EXPERIMENTAL_FEATURES=y

# === split BLE 재연결 신뢰성 (#1, urob 컨센서스) ===
# 재연결 후 central이 peripheral GATT notification 구독을 못 살려 통신이 끊긴 채
# 복구가 안 되는 문제 완화. (오른쪽 간헐 멈춤이 BLE 재연결 실패 계열이면 직접 효과)
CONFIG_BT_GATT_ENFORCE_SUBSCRIPTION=n

# === 분리형 키보드 배터리 ===
# dongle central + 트랙볼 peripheral 구조라 #3156 (트랙볼-central 키 입력 누락) 영향 없음
# 좌/우 배터리 정보를 dongle이 polling해서 host에 proxy → macOS BT 패널/OLED에 표시
CONFIG_ZMK_SPLIT_BLE_CENTRAL_BATTERY_LEVEL_FETCHING=y
CONFIG_ZMK_SPLIT_BLE_CENTRAL_BATTERY_LEVEL_PROXY=y
