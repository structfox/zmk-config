# === Dongle 전용 설정 ===
# 공통 설정은 config/corne_xiao.conf에서 자동으로 로드됨
# 여기에는 Dongle 전용 설정 또는 오버라이드만 작성

# === USB 우선 (Dongle 전용) ===
CONFIG_ZMK_USB=y

# === 디버깅 (필요시 활성화) ===
# CONFIG_ZMK_USB_LOGGING=y
                   