# ==========================================================================
# Charybdis 공통 설정 (키맵 베이스명 conf — ZMK가 left/right/dongle 빌드 모두에 머지).
# 주의: boards/shields/charybdis/charybdis.conf 는 shield 이름과 안 맞아 머지되지 않음.
#       공통 설정은 반드시 이 파일(config/charybdis.conf)에 둘 것.
# ==========================================================================

# === USB / 입력 ===
CONFIG_ZMK_USB=y
CONFIG_ZMK_POINTING=y
# macOS에서 HID Resolution Multiplier로 마우스 입력이 멈추는 문제로 비활성화
CONFIG_ZMK_POINTING_SMOOTH_SCROLLING=n

# === 디바운스 ===
CONFIG_ZMK_KSCAN_DEBOUNCE_PRESS_MS=5
CONFIG_ZMK_KSCAN_DEBOUNCE_RELEASE_MS=5

# ==========================================================================
# === ESB Split Transport (트랙볼 반응속도 향상) ===
# BLE 스택을 들어내고 Nordic ESB(TDMA + FHSS)로 좌/우 peripheral 통신.
# central(dongle)은 USB로만 host 연결, peripheral은 ESB로만 central 연결.
# ==========================================================================

# --- BLE 완전 비활성화 + 기본 split transport 제거 ---
CONFIG_ZMK_BLE=n
CONFIG_ZMK_SPLIT_BLE=n
CONFIG_ZMK_SPLIT_WIRED=n

# --- ESB split transport 활성화 (CONFIG_ESB 는 자동 select) ---
CONFIG_ZMK_SPLIT_ESB=y

# --- ESB 핵심 파라미터 ---
# 32.768kHz 외부 크리스탈 사용 (nice_nano v2 탑재). TDMA 타이밍 정확도에 중요.
CONFIG_CLOCK_CONTROL_NRF_K32SRC_XTAL=y
# 페이로드 최대 길이 (모듈 요구치 >= 48). 트랙볼 input_event forwarding 여유 확보.
CONFIG_ESB_MAX_PAYLOAD_LENGTH=48
# TX ring buffer 경로 (모듈 기본값)
CONFIG_ESB_TX_RINGBUF=y
# 2Mbps PHY
CONFIG_ZMK_SPLIT_ESB_BITRATE_2MBPS=y
# 폴링 레이트 1kHz: peripheral 2개 → 각 반쪽 슬롯이 2ms(=500Hz)마다.
# 기존 BLE 7.5ms(125Hz cap) 대비 4배. 배터리/반응속도 균형점.
CONFIG_ESB_POLLING_RATE_1KHZ=y

# === 배터리 보고 (BLE off 시 모듈 Kconfig가 동일 심볼 재정의) ===
CONFIG_ZMK_BATTERY_REPORTING=y
